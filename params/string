:
# A script which starts the splines program up in curve_erase mode.
# In order to run this on the root window, invoke as "string -R"
# To run in demo mode, use "string -l 4096" or some such

splines -e $*
