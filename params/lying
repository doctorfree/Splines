:
# A script which starts the splines program up in delayed erase mode.
# In order to run this on the root window, invoke as "lying -R"
# To run in demo mode, use "lying -l 4096" or some such

splines -L -N 16 -M 48 $*
